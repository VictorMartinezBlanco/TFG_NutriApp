import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function CalendarPage() {
  return (
    <PagePlaceholder
      title="Calendar"
      note="The calendar needs its own tables before it can show real data."
    />
  );
}
