import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function MessagesPage() {
  return (
    <PagePlaceholder
      title="Messages"
      note="Messaging needs its own tables before it can show real data."
    />
  );
}
