import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function PlansPage() {
  return (
    <PagePlaceholder
      title="Plans"
      note="The plan generator and uploader land in a later block."
    />
  );
}
