services:
  - type: web
    name: nutriapp-api
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    region: frankfurt
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL_POOLER
        sync: false
      - key: NUTRIAPP_API_TOKEN
        sync: false
